// Shared types between client and server.
// Phase 2 will migrate types from src/client/lib/api.ts and src/server/routes/*.ts here.
// For now, both sides continue using their own type definitions.
export {}
